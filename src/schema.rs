table! {
    visitor(id){
        id -> Int4,
        name -> Varchar,
        username -> Varchar,
        password -> Varchar,
        phone_number -> Varchar,
        accepted_terms -> Bool,
        accepted_comercial -> Bool,
        user_type -> Bool,
    }
}